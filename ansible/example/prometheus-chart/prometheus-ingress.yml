apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: prometheus
spec:
  rules:
  - host: prometheus.k8s.apicat.xyz
    http:
      paths:
      - path: /
        backend:
          serviceName: prometheus-server
          servicePort: 80
