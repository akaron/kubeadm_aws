kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: jenkins-home
  annotations:
    volume.beta.kubernetes.io/storage-class: "standard-a"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 13Gi
